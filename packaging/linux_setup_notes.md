# Linux setup notes

1. Cài Python 3.11+, `python3-venv`, Node.js LTS, FFmpeg và `xdg-utils`.
2. Chạy `chmod +x scripts/*.sh`.
3. Chạy `./scripts/check_system.sh`.
4. Chạy `./scripts/start_local_app.sh`.

Tính năng Open Folder dùng `xdg-open`. App mặc định chỉ bind `127.0.0.1`.
