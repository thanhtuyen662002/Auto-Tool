# Auto Tool v0.2 Bug Bash

For each case, record the expected behavior, actual behavior, and pass/fail
result. Add screenshots, terminal output, and output folder paths when useful.

## Cases

### Source folder does not exist

Expected behavior:

Actual behavior:

Pass/Fail:

### Source folder is empty

Expected behavior:

Actual behavior:

Pass/Fail:

### Video input is too short

Expected behavior:

Actual behavior:

Pass/Fail:

### Landscape 16:9 video

Expected behavior:

Actual behavior:

Pass/Fail:

### Square 1:1 video

Expected behavior:

Actual behavior:

Pass/Fail:

### Video has no audio

Expected behavior:

Actual behavior:

Pass/Fail:

### Gemini API key is missing

Expected behavior:

Actual behavior:

Pass/Fail:

### Edge-TTS network error

Expected behavior:

Actual behavior:

Pass/Fail:

### Piper model is missing

Expected behavior:

Actual behavior:

Pass/Fail:

### Subtitle is too long

Expected behavior:

Actual behavior:

Pass/Fail:

### Product info has no brand

Expected behavior:

Actual behavior:

Pass/Fail:

### Product info has risky claims

Expected behavior:

Actual behavior:

Pass/Fail:

### Almost all segments are excluded

Expected behavior:

Actual behavior:

Pass/Fail:

### Cache JSON is corrupt

Expected behavior:

Actual behavior:

Pass/Fail:

### Output folder has no write permission

Expected behavior:

Actual behavior:

Pass/Fail:

### Rerender is requested when old script is missing

Expected behavior:

Actual behavior:

Pass/Fail:

### Content export is requested when caption is missing

Expected behavior:

Actual behavior:

Pass/Fail:
